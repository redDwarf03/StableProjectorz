# Every launch, StableProjectorz will check if a new version is available.
# It will check these 2 URLS:
#
#  https://pastebin.com/raw/DP5JQ5Ja
#  https://stableprojectorz.com/latest-version
#
# But if you want to skip checking these URLs, remove the # character:

#--skip-updates-check


# ---------------------------------------------------------------------------
# Agent bridge (optional, OFF by default)
#
# Opens a local command socket on 127.0.0.1 so an external tool - such as the
# spz-mcp server - can inspect the app and trigger actions.
# It never listens on anything but the loopback address, but any program
# running on this machine could still talk to it. Leave it off unless you use it.
#
# Remove the # character to enable:

#--agent-bridge

# Optional. Default port is 8765:
#--agent-bridge-port=8765

# Optional shared secret. When set, every request must carry the same token:
#--agent-bridge-token=change-me


# ---------------------------------------------------------------------------
# Guidance for Flux checkpoints
#
# Flux models already have the effect of CFG baked into their weights, and read
# their guidance from a separate 'distilled_cfg_scale' field instead. So when
# the loaded checkpoint looks like a Flux one, the CFG slider is sent in that
# field and cfg_scale is pinned to 1 - otherwise webui runs its two-pass CFG on
# top, which doubles the generation time and burns the colours.
#
# The checkpoint is recognised by its filename, so an unusually named model can
# be missed - or a non-Flux model with 'flux' in its name wrongly caught.
# Remove the # character to switch this off and always send cfg_scale:

#--no-distilled-cfg